import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const OurHeroesScreen = (props: Props) => {
  return (
    <View>
      <Text>OurHeroesScreen</Text>
    </View>
  );
};

export default OurHeroesScreen;

const styles = StyleSheet.create({});
