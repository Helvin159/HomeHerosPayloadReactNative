import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const SalesPipeline = (props: Props) => {
  return (
    <View>
      <Text>SalesPipeline</Text>
    </View>
  );
};

export default SalesPipeline;

const styles = StyleSheet.create({});
